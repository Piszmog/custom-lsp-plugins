# templ-lsp

Templ language server for Go HTML templating code intelligence in Claude Code.

## Install the binary

Mason (Neovim): `:MasonInstall templ`
Go: `go install github.com/a-h/templ/cmd/templ@latest`

The `templ` binary must be on your PATH.
