# htmx-lsp

HTMX language server for hx-* attribute completions in Claude Code.

## Install the binary

Mason (Neovim): `:MasonInstall htmx-lsp`
Cargo: `cargo install htmx-lsp`

The `htmx-lsp` binary must be on your PATH.
