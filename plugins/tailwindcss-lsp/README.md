# tailwindcss-lsp

TailwindCSS language server for class completions and diagnostics in Claude Code.

## Install the binary

Mason (Neovim): `:MasonInstall tailwindcss-language-server`
npm: `npm install -g @tailwindcss/language-server`

The `tailwindcss-language-server` binary must be on your PATH.
